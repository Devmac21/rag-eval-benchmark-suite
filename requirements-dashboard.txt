# Optional UI — keep requirements.txt slim for Docker / API installs.
# Usage: pip install -r requirements.txt -r requirements-dashboard.txt
# Or: pip install -e ".[dashboard]"
streamlit>=1.28
plotly>=5.18
pypdf>=4.0
python-docx>=1.0
