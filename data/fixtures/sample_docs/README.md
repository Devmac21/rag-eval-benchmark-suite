# Sample documents for pipeline smoke tests

Synthetic **`.txt`** files (not real NYU material) used by
`scripts/smoke_dashboard_documents.py` — same path as the dashboard: **load text → chunk → build pack → BM25 benchmark**.

Add more `*.txt` here to exercise multi-doc retrieval.
